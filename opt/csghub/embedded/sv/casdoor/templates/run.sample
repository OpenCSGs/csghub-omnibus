#!/bin/bash

# Redirect stderr -> stdout
exec 2>&1

# fail on errors
set -e

{{- $dataDir := (datasource "config").casdoor.data | default "/var/opt/csghub/casdoor" }}
cd {{ $dataDir }}
exec chpst -e /opt/csghub/etc/casdoor/env -P \
  -U casdoor:casdoor \
  -u casdoor:casdoor \
  /opt/csghub/embedded/bin/casdoor

